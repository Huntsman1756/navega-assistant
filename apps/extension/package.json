{
  "name": "@guided-web/extension",
  "version": "0.0.1-p0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "lint": "eslint src"
  },
  "dependencies": {
    "@guided-web/accessible-dom": "workspace:*",
    "@guided-web/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "esbuild": "^0.28.1",
    "@playwright/test": "^1.47.0",
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.5.0",
    "vitest": "^2.0.5"
  }
}
