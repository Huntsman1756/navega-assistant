{
  "name": "@guided-web/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"extension: e2e via playwright\"",
    "test:unit": "echo \"extension: no unit tests\"",
    "test:e2e": "playwright test",
    "lint": "echo \"extension: no lint configured\""
  },
  "dependencies": {
    "@guided-web/accessible-dom": "workspace:*",
    "@guided-web/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "esbuild": "^0.23.0",
    "@playwright/test": "^1.47.0",
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.5.0",
    "vitest": "^2.0.5"
  }
}
